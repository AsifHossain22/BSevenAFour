model ServiceCategory {
  id String @id @default(uuid())
  categoryName String @unique
  categoryDescription String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  services Service[]

  @@map("service_ategories")
}